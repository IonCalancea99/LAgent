---
topic: LAgent PRD
updated: 2026-08-04T17:18
---

- (decision) Working mode: Fast path — brief + addendum used as source, ASSUMPTION tags placed on 8 unconfirmed items
- (decision) Behavior policy: FSM-first for V1; BC policy deferred per Rule of Three
- (decision) YOLO strategy: assumed separate models per behavior profile (Fishing vs Combat) — open question in §8
- (decision) Annotation tool: assumed Label Studio for Python API integration — open question in §8
- (decision) Session persistence: assumed session-fresh (no cross-session zone memory) — open question in §8
- (decision) Q1 resolved: Combined FSM+BC policy — FSM for state transitions, BC Policy for within-state action selection
- (decision) Q2 resolved: Dual YOLO model families — Common Model (class-agnostic) + Class Model (class-specific), both run per frame
- (decision) Q3 resolved: PP buff cycle is timer-driven; all timed casts gated by Safety Check (aggro-risk radius + WL state)
- (decision) Q4 resolved: Label Studio confirmed as annotation tool
- (decision) Q5 resolved: EasyOCR default pre-trained weights, no fine-tuning required
- (decision) Q6 resolved: Asterios x55 does not support offline dancer mode — BD/SWS integration not planned
- (decision) Q7 resolved: Session-fresh confirmed — no cross-session zone memory
- (decision) Q8 resolved: Shadow Mode included in V1 as FR-24 (good to have)
