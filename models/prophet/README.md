# Prophet model

Place the Prophet-specific YOLO weights at `models/prophet/current.pt`.

This model is loaded when the Prophet agent requests inference. It is optional
while developing or running tests.